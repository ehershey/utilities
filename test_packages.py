# !/usr/bin/python
# spin up ubuntu machine
# install pre-req packages
# install gpg key
# clone repo
# tag repo
# run packager.py 
# copy repo into place
# run packager-enterprise.py into /tmp
# copy repo into place
# iterate through distros running all tests

